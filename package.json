{
  "name": "@apex-ai/text-layout-engine",
  "version": "1.0.0",
  "description": "Deterministic text layout engine without DOM measurement — shared across Apex AI harnesses",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "demo": "tsc && node dist/demo.js"
  },
  "files": [
    "dist",
    "src"
  ],
  "dependencies": {
    "@napi-rs/canvas": "^0.1.65"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  }
}
